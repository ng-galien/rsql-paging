spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/rsqlpaging
    username: rsqlpaging
    password: rsqlpaging
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    open-in-view: false
    defer-datasource-initialization: true
  sql:
    init:
      mode: always
